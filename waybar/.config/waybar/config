{
    "layer": "top",
    "modules-left": ["hyprland/workspaces"],
    "modules-center": [
	    "clock#date",
	    "clock#time"
    ],
    "modules-right": [
	    "pulseaudio",
	    "custom/poweroff",
	    "tray"
    ],
    "clock#date": {
        "format": "{:%a, %b %d}", // Example: Mon, Oct 20
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "interval": 60 // Update every 60 seconds
    },
    "clock#time": {
        "format": " - {:%H:%M}", // Example: 12:54 AM (12-hour format)
        "interval": 1 // Update every second
    },
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "tooltip": true,
        "format-muted": " Muted",
        "format-icons": {
            "default": ["", ""], // Icons for low and high volume
            "muted": ""
        },
        // Action to open pavucontrol when the volume module is clicked
        "on-click": "pavucontrol" 
    },
    "custom/poweroff": {
        "format": "⏻ ", // An icon for power (e.g., Power button icon from Nerd Fonts)
        "tooltip": "Power Off PC",
        "on-click": "systemctl poweroff"
    }
}
